# Deliberately minimal. The bot talks to the Telegram Bot API directly over
# aiohttp rather than through a framework — the frameworks pull in pydantic,
# which has no wheel for the newest Python releases and fails to compile.
# Everything here has prebuilt wheels or is pure Python.
aiohttp>=3.10
cryptography>=43.0
python-dotenv>=1.0
telethon>=1.36
